#include<stdio.h>
int main()
{
	int x, y, z;
	printf("Enter the x value =");
	scanf("%d",&x);
	printf("Enter the y value =");
	scanf("%d" ,&y);
	z = x;
	x = y;
	y = z;
	printf("x = %d\n",x);
	printf("y = %d\n",y);
	return 0;
}
